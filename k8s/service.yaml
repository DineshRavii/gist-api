apiVersion: v1
kind: Service
metadata:
  name: gist-api
  labels:
    app: gist-api
spec:
  type: NodePort
  selector:
    app: gist-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080